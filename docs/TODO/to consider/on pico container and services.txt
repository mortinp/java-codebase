-Se podran lograr consultas genericas usando pico container???... Por ejemplo, poder decir:


Person director = new Director(1, "director #1");
Person actor = new Actor(1, "actor #1");
List filmsByDirector  = filmsGateway.findBy(director);
List filmsByActor  = filmsGateway.findBy(actor);

Aqui el metodo findBy es un metodo generico que usa un pico container para enlazar servicios especificos. Por ejemplo:

public class FilmsGateway {
	FilmsServices filmsServices;
	PicoContainer container;

	public List configure() {
		//configure container	
	}

	public List findBy(Object ... discriminators) {
		//execute proper finder method using the pico container configuration (how???... I don't know!!!)
	}
}

public class FilmsServices {

	public List findByDirector() {
		//some implementation
	}

	public List findByActor() {
		//some implementation
	}

}

UPDATE (17-02-2013): Esto se puede lograr si se conoce el nombre del campo en la tabla 'films', que referencia a los actores o a los directores (tambien hay que tener en cuenta la multiplicidad de cada campo: onteToMany, OneToOne, etc.). Si supieramos esto, entonces se puede crear un filtro que iguale dicho campo, con el campo de la tabla 'actors' o 'directors', y esto se puede conocer con los DAOs de 'actors' y 'directors'.

